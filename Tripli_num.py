# Lista de números
numeros = [1, 2, 3, 4, 5]

# Usar map() para triplicar cada número
triplicados = list(map(lambda x: x * 3, numeros))

# Imprimir la lista resultante
print("Lista original:", numeros)
print("Lista triplicada:", triplicados)